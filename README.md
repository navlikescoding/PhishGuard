# PhishGuard
PhishGuard is an ML-based phishing URL detection system built in R, trained on 822,010 URLs using Logistic Regression, Random Forest, XGBoost, and LiblineaR. XGBoost achieved the best AUC of 0.971. Deployed as a real-time Shiny web app where users can instantly classify any URL as phishing or legitimate.
